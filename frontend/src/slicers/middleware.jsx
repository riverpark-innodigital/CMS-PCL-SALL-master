// const trackGetAllProductsMiddleware = (store) => (next) => (action) => {
//     if (action.type === getAllProducts.pending.type) {
//         console.log('getAllProducts was called', action);
//     }
//     return next(action);
// };

// export default trackGetAllProductsMiddleware